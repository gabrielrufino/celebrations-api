name: Continuous Integration

on:
  - push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          docker image build \
            -t celebrations-api:test \
            .
